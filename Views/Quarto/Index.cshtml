@model IEnumerable<Quarto>

@{
    ViewData["Title"] = "Privacy Policy";
}
<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-9">
                <h1>Listagem de Quartos</h1>
            </div>
            <div class="col-sm-3 text-right">
                <a asp-controller="Quarto" asp-action="Create" class="btn btn-primary ">Adicionar</a>
               
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Visualise todos os quartos cadastrados</h3>
                </div>
                <!-- /.card-header -->
                <div class="card-body">
                    <table id="example1" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Número</th>
                                <th>Andar</th>
                                <th>Tipo</th>
                                <th>Ocupado</th>
                                <th></th>
                            </tr>
                        </thead>

                        <tbody>
                          @foreach (Quarto item in Model)
	                       {
                            <tr>
                                <td>@item.Id</td>
                                <td>@item.Numero</td>
                                <td>@item.Andar</td>
                                <td>@item.Tipo</td>
                                <td>@item.Ocupado</td>
                                <td>
                                    <a asp-action="Edit" asp-controller="Quarto" asp-route-id="@item.Id" class="btn btn-success text-white">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a asp-action="Delete" asp-controller="Quarto" asp-route-id="@item.Id" class="btn btn-danger text-white">
                                        <i class="fas fa-trash-alt"></i>
                                    </a>
                                </td>

                            </tr>
	                       }
                            
                        </tbody>
                        
                    </table>
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</section>
<!-- /.content -->